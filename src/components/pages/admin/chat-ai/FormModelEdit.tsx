import React from "react";
import FormModelAdd from "./FormModelAdd";

// Reusing the same form structure for edit
const FormModelEdit = () => {
    return <FormModelAdd />;
};

export default FormModelEdit;
